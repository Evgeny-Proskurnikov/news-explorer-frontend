import React from 'react';
import SearchForm from '../SearchForm/SearchForm';

function Main({ children }) {
  return (
    <div className='main'>
      <SearchForm />
    </div>
  )
}

export default Main;
